{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "abi": {
      "type": "local",
      "command": ["abi-mcp", "--profile", "safe"],
      "enabled": true,
      "timeout": 10000
    }
  }
}
