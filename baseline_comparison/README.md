# Baseline Comparison

This directory indexes experimental baseline outputs for:

- README-only agents
- plain Python API agents
- plain tool-calling agents
- ABI control-layer agents

The detailed experiment scaffold lives in `docs/experiments/`.
