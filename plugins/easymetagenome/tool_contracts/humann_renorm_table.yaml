abi_version: "0.1"
tool_id: humann_renorm_table
name: HUMAnN Renormalize Table
category: functional_summary
purpose: Normalize a HUMAnN cohort matrix to relative abundance.
inputs: {input_table: {type: file, formats: [tsv], required: true}}
outputs: {normalized_table: {type: file, format: tsv}}
execution: {executable: humann_renorm_table, network: false, writes_output: true}
normalization: {tables: [functional_abundance]}
failure_handling: {tool_not_found: {hint: Create the easymeta-humann environment.}}
resources: {cpu: 1, memory: 8GB, walltime: "02:00:00"}
